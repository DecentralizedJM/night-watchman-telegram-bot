# Night Watchman Bot Dependencies

# HTTP requests (async)
httpx>=0.25.0

# Environment variables
python-dotenv>=1.0.0

# ML-based spam detection
scikit-learn>=1.3.0
sentence-transformers>=2.2.0
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu

# OpenAI GPT API (spam detection brain)
openai>=1.12.0

# Note: Hugging Face API uses httpx (already included above)
# Optional: Get free API key at https://huggingface.co/settings/tokens

# Redis (for state management)
redis>=5.0.0
